<form method="post" action="{{ path('app_specialty_domain_spell_table_delete', {'id': domain_spell_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ domain_spell_table.id) }}">
    <button class="btn">Delete</button>
</form>
