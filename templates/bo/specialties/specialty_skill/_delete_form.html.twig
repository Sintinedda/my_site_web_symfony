<form method="post" action="{{ path('app_specialty_skill_delete', {'id': specialty_skill.id, 'slug': specialty.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialty_skill.id) }}">
    <button class="btn">Delete</button>
</form>
