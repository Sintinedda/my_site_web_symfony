<form method="post" action="{{ path('app_specialty_skill_table_delete', {'id': specialty_skill_table.id, 'id2': skill.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialty_skill_table.id) }}">
    <button class="btn">Delete</button>
</form>
