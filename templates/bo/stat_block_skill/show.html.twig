{% extends 'base.html.twig' %}

{% block title %}StatBlockSkill{% endblock %}

{% block body %}
    <h1>StatBlockSkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stat_block_skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stat_block_skill.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ stat_block_skill.descr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stat_block_skill_index') }}">back to list</a>

    <a href="{{ path('app_stat_block_skill_edit', {'id': stat_block_skill.id}) }}">edit</a>

    {{ include('stat_block_skill/_delete_form.html.twig') }}
{% endblock %}
