{% extends 'base.html.twig' %}

{% block title %}SpellSchool{% endblock %}

{% block body %}
    <h1>SpellSchool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spell_school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spell_school.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ spell_school.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spell_school_index') }}">back to list</a>

    <a href="{{ path('app_spell_school_edit', {'id': spell_school.id}) }}">edit</a>

    {{ include('spell_school/_delete_form.html.twig') }}
{% endblock %}
