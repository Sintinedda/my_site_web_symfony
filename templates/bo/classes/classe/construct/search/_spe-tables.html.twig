<div>
    <input type="text" onkeyup="search('search-spe-table-{{ classe.id }}-{{ specialty.id }}', 'liste-spe-table-{{ classe.id }}-{{ specialty.id }}')"  id="search-spe-table-{{ classe.id }}-{{ specialty.id }}" placeholder="chercher"/>
    <ul id="liste-spe-table-{{ classe.id }}-{{ specialty.id }}">
        {% for s in speTables %}
            <li style="display: none;"><span>{{ s.name }}</span> &nbsp; <a href="{{ path('app_skill_edit', {'id': s.id, 'slug': specialty.slug}) }}">Éditer</a></li>
        {% endfor %}
    </ul>
</div>