<form method="post" action="{{ path('app_spell_list_delete', {'id': spell_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spell_list.id) }}">
    <button class="btn">Delete</button>
</form>
