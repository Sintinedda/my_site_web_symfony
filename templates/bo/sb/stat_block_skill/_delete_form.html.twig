<form method="post" action="{{ path('app_stat_block_skill_delete', {'id': stat_block_skill.id, 'slug': sb.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stat_block_skill.id) }}">
    <button class="btn">Delete</button>
</form>
