<form method="post" action="{{ path('app_sb_delete', {'id': sb.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sb.id) }}">
    <button class="btn">Delete</button>
</form>
