<form method="post" action="{{ path('app_sb_reaction_delete', {'id': sbReaction.id, 'slug': sb.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sbReaction.id) }}">
    <button class="btn">Delete</button>
</form>
