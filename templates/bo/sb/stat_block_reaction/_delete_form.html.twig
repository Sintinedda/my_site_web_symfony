<form method="post" action="{{ path('app_stat_block_reaction_delete', {'id': stat_block_reaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stat_block_reaction.id) }}">
    <button class="btn">Delete</button>
</form>
