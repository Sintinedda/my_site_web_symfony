<form method="post" action="{{ path('app_skill_delete', {'id': skill.id, 'slug': classe.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill.id) }}">
    <button class="btn">Delete</button>
</form>
