{% extends 'base.html.twig' %}

{% block title %}StatBlockAction{% endblock %}

{% block body %}
    <h1>StatBlockAction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stat_block_action.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stat_block_action.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ stat_block_action.descr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stat_block_action_index') }}">back to list</a>

    <a href="{{ path('app_stat_block_action_edit', {'id': stat_block_action.id}) }}">edit</a>

    {{ include('stat_block_action/_delete_form.html.twig') }}
{% endblock %}
