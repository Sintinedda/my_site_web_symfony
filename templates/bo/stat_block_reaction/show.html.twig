{% extends 'base.html.twig' %}

{% block title %}StatBlockReaction{% endblock %}

{% block body %}
    <h1>StatBlockReaction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stat_block_reaction.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stat_block_reaction.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ stat_block_reaction.descr }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stat_block_reaction_index') }}">back to list</a>

    <a href="{{ path('app_stat_block_reaction_edit', {'id': stat_block_reaction.id}) }}">edit</a>

    {{ include('stat_block_reaction/_delete_form.html.twig') }}
{% endblock %}
