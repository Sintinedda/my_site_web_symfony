{% extends 'base.html.twig' %}

{% block title %}Classe{% endblock %}

{% block body %}
    <h1>Classe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ classe.name }}</td>
            </tr>
            <tr>
                <th>Dv</th>
                <td>{{ classe.dv }}</td>
            </tr>
            <tr>
                <th>Armor</th>
                <td>{{ classe.Armor }}</td>
            </tr>
            <tr>
                <th>Weapon</th>
                <td>{{ classe.weapon }}</td>
            </tr>
            <tr>
                <th>Tool</th>
                <td>{{ classe.tool }}</td>
            </tr>
            <tr>
                <th>Save</th>
                <td>{{ classe.save }}</td>
            </tr>
            <tr>
                <th>Competence</th>
                <td>{{ classe.competence }}</td>
            </tr>
            <tr>
                <th>Equipment</th>
                <td>{{ classe.equipment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classe_index') }}">back to list</a>

    <a href="{{ path('app_classe_edit', {'id': classe.id}) }}">edit</a>

    {{ include('bo/classe/_delete_form.html.twig') }}
{% endblock %}
