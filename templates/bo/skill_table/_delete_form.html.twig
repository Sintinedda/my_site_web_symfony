<form method="post" action="{{ path('app_skill_table_delete', {'id': skill_table.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill_table.id) }}">
    <button class="btn">Delete</button>
</form>
