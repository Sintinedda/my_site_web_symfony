{% extends 'base.html.twig' %}

{% block title %}SpecialtyItem{% endblock %}

{% block body %}
    <h1>SpecialtyItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialty_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialty_item.name }}</td>
            </tr>
            <tr>
                <th>Descr1</th>
                <td>{{ specialty_item.descr1 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialty_item_index') }}">back to list</a>

    <a href="{{ path('app_specialty_item_edit', {'id': specialty_item.id}) }}">edit</a>

    {{ include('specialty_item/_delete_form.html.twig') }}
{% endblock %}
