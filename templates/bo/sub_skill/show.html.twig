{% extends 'base.html.twig' %}

{% block title %}SubSkill{% endblock %}

{% block body %}
    <h1>SubSkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_skill.id }}</td>
            </tr>
            <tr>
                <th>Title1</th>
                <td>{{ sub_skill.title1 }}</td>
            </tr>
            <tr>
                <th>Descr_one</th>
                <td>{{ sub_skill.descrOne }}</td>
            </tr>
            <tr>
                <th>Descr_one2</th>
                <td>{{ sub_skill.descrOne2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sub_skill_index') }}">back to list</a>

    <a href="{{ path('app_sub_skill_edit', {'id': sub_skill.id}) }}">edit</a>

    {{ include('sub_skill/_delete_form.html.twig') }}
{% endblock %}
