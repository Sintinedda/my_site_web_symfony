{% extends 'base.html.twig' %}

{% block title %}SpecialtySkill{% endblock %}

{% block body %}
    <h1>SpecialtySkill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialty_skill.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialty_skill.name }}</td>
            </tr>
            <tr>
                <th>Descr1</th>
                <td>{{ specialty_skill.descr1 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_specialty_skill_index') }}">back to list</a>

    <a href="{{ path('app_specialty_skill_edit', {'id': specialty_skill.id}) }}">edit</a>

    {{ include('specialty_skill/_delete_form.html.twig') }}
{% endblock %}
